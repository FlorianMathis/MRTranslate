using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DataPresentation : MonoBehaviour
{
    // Start is called before the first frame update
    public int condition;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
